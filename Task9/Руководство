Тут мы не пользуемся богомерзким экселем для решения задания, мы просто копируем все строки и столбцы таблицы, кидаем это всё в txt-шник, меняем раделение на ";".
Далее просто открываем этот файл через питон и прописываем условия (внимательно читаем, что у нас просят ОДНО или ВСЕ условия - это важно)
Для вскрытия и работы с файлом используем такой код:
f = open("9 (5).txt")
r =f.readlines()
c=0
for t in r:
    a=list(map(int,t.split(';')))

И радуемся жизни